# Copy this file to `.env` (same folder) and fill in the paths on YOUR machine.
# `.env` is git-ignored, so real paths never reach GitHub.
# Independent of the root fishbehavior package's FISH_* vars (PRD Clarification C10).

# Folder that contains the local video mirror, organized as
# {compound}/{sex}_{subject_number}.mp4 (see PRD C11/C15 - OneDrive synced locally).
PDS_VIDEO_DIR=data/videos

# The trial database workbook (00_NTT_DataBase.xlsx).
PDS_DB_PATH=data/00_NTT_DataBase.xlsx

# Folder holding the two restricted reference PNGs used only by one-time calibration.
PDS_REFERENCE_DIR=data/reference

# Where per-video working artifacts go (PROCESSED_AUTO/EDITED). Default: outputs
PDS_OUTPUT_DIR=outputs

# Where the accepted/gold dataset + accepted_index.parquet go. Default: accepted
PDS_ACCEPTED_DIR=accepted

# Number of videos processed in parallel by `prepds run`. Default: max(1, cpu_count - 2)
PDS_WORKERS=1

# Optional: YAML file whose values override config/default_thresholds.yaml
# PDS_CONFIG=my_thresholds.yaml
